<div class="container mx-auto">
  <h1 class="text-3xl font-bold text-center mt-8 mb-6 mx-4">ALL Articles</h1>

    <div>
    <%= render partial: "search/form", locals: { results: [] } %>
  </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 lg:mx-10">
      <% @articles.each do |article| %>
          <%= render article %>
      <% end %>
    </div>
</div>
